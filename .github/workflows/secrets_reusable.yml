name: Secrets | Reusable Workflow Demo

on:
  workflow_dispatch:
    inputs:
      username:
        required: true
        type: string

jobs:
  gitHub-actions-demo:
    uses: dsrinivasa/reusable-wf/.github/workflows/secrets_test.yml@main
    with:
      username: test
    secrets:
      token: ${{ secrets.GITHUB_TOKEN }}
    
